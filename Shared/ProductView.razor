@inject BasketService BasketService

@* UI Section *@
<div class="card w-50">
    <div class="card-header">
        @product.Name
    </div>
    <div class="card-body">

        <h5 class="card-title">Product @product.Id : @product.Name</h5>
        <p class="card-text">@product.Description</p>
        <blockquote class="card-text"><strong>£@product.Price</strong></blockquote>

        @if (DisplayBuyButton)
        {
            <button class="btn btn-primary" type="button"
                @onclick="()=> Buy(product.Id)">Buy</button>
        }

    </div>
</div>

@* Code Section *@
@code 
{
    [Parameter]
    public Product product { get; set; } = new Product();

    [Parameter]
    public bool DisplayBuyButton { get; set; } = true;

    private void Buy(int productId)
    {
        BasketService.AddToBasket(productId);
    }
}